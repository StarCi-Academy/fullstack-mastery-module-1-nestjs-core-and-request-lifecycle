# Default port 3000; overridable via -Dserver.port or the PORT env var.
server.port=${PORT:3000}
# Bind to loopback only so the lesson server is not exposed on the network.
server.address=127.0.0.1
# Disable the banner for cleaner logs while observing the lifecycle.
spring.main.banner-mode=off
